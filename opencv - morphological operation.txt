Morphological operation
---------------------------

-> https://docs.opencv.org/4.5.2/d9/d61/tutorial_py_morphological_ops.html

Erosion :
erosion = cv.erode(img,kernel,iterations = 1)
eroded = cv2.erode(img, None, iterations=i + 1)

Dilatation :
dilated = cv2.dilate(img, None, iterations=i + 1)

Opening (erosion + dilation) : 
opening = cv2.morphologyEx(img, cv2.MORPH_OPEN, kernel)

Closing (dilation + erosion) : 
closing = cv2.morphologyEx(img, cv2.MORPH_CLOSE, kernel)

Gradient (difference between dilation and erosion) :
gradient = cv2.morphologyEx(img, cv2.MORPH_GRADIENT, kernel)

Top / White Hat (difference between original and opening) :

Black Hat (difference between original and closing) :
blackhat = cv2.morphologyEx(img, cv2.MORPH_BLACKHAT, rectKernel)

Kernel :
kernel = np.ones((5,5),np.uint8)
kernel = cv2.getStructuringElement(cv2.MORPH_RECT, kernelSize)
rectKernel = cv2.getStructuringElement(cv2.MORPH_RECT, (13, 5))
